[tox]
envlist = py27
[testenv]
deps =
    unittest2
    nose
    coverage
    mock
    numpy
    python-snappy
    twisted
commands =
    nosetests --with-coverage --cover-erase --cover-package afkak []
setenv =
    PROJECT_ROOT = {toxinidir}
